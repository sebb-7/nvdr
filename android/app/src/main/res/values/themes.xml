<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window chrome only; Compose + MaterialTheme draw the actual UI. -->
    <style name="Theme.FarRelay" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/background_light</item>
    </style>
</resources>
